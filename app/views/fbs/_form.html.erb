<%= form_for(@fb) do |f| %>
  <% if @fb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fb.errors.count, "error") %> prohibited this fb from being saved:</h2>

      <ul>
      <% @fb.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
